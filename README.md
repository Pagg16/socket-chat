# 🚀 Socket-Chat: Real-Time Messaging Platform

## 📖 Описание проекта

**Socket-Chat** — это полнофункциональное веб-приложение для обмена сообщениями в реальном времени, построенное на современном стеке MERN (MongoDB, Express, React, Node.js) с использованием Socket.io для двусторонней связи. Приложение предоставляет функционал личных и групповых чатов с расширенными возможностями управления пользователями и сообщениями.

## 🎯 Цели проекта

1. **Реализовать систему мгновенного обмена сообщениями** с поддержкой личных и групповых чатов
2. **Обеспечить работу в реальном времени** через WebSocket-подключения
3. **Создать удобный интерфейс** с адаптивным дизайном и интуитивным управлением
4. **Реализовать систему управления пользователями** с аутентификацией и профилями
5. **Обеспечить безопасность данных** с использованием JWT токенов

## ✨ Ключевые возможности

### 📱 Основной функционал

- **Регистрация и авторизация** пользователей с загрузкой аватаров
- **Поиск пользователей** по имени или email
- **Создание чатов** (личные и групповые)
- **Отправка сообщений** в реальном времени
- **Индикаторы набора текста** (typing indicators)
- **Отображение непрочитанных сообщений**
- **Сортировка чатов** по последней активности

### 👥 Управление чатами

- **Создание групповых чатов** с названием и участниками
- **Изменение названия** групповых чатов (для администраторов)
- **Добавление/удаление пользователей** из групповых чатов
- **Выход из группового чата**
- **Обновление информации о чатах** в реальном времени для всех участников

### 👤 Управление профилем

- **Изменение имени и email**
- **Загрузка и обновление аватара**
- **Отображение текущей статистики**

## 🛠️ Технологический стек

### Frontend (Client)

- **React 18+** — библиотека для построения пользовательских интерфейсов
- **React Router DOM** — маршрутизация внутри приложения
- **Axios** — HTTP-клиент для взаимодействия с API
- **Socket.io Client** — клиентская часть для работы с WebSocket
- **CSS Modules** — изолированные стили компонентов
- **Context API** — управление состоянием приложения
- **React Hooks** — функциональные компоненты с хуками

### Backend (Server)

- **Node.js** — среда выполнения JavaScript
- **Express.js** — фреймворк для создания REST API
- **Socket.io** — библиотека для работы с WebSocket
- **MongoDB + Mongoose** — база данных и ODM
- **JWT (JSON Web Tokens)** — аутентификация и авторизация
- **Bcrypt** — хеширование паролей
- **Express Validator** — валидация входных данных
- **Multer** — обработка загрузки файлов
- **CORS** — обработка кросс-доменных запросов

### Инфраструктура

- **MongoDB** — NoSQL база данных для хранения пользователей, чатов и сообщений
- **WebSocket** — протокол для двусторонней связи в реальном времени
- **JWT Tokens** — безопасное хранение сессий пользователей

## 📁 Архитектура проекта

### Frontend (client/)

```
socket-chat/client/
├── public/                    # Статические файлы
├── src/
│   ├── api/                  # API запросы (api.js)
│   ├── components/           # React компоненты
│   │   ├── App/             # Корневой компонент
│   │   ├── ButtonLoader/    # Индикатор загрузки кнопок
│   │   ├── ChatBox/         # Основной интерфейс чата
│   │   ├── Chats/           # Контейнер чатов
│   │   ├── GroupChatModal/  # Модальное окно групповых чатов
│   │   ├── InputImage/      # Компонент загрузки изображений
│   │   ├── Loader/          # Компонент загрузки
│   │   ├── LoginSingUp/     # Страница входа/регистрации
│   │   ├── MyChats/         # Список чатов пользователя
│   │   ├── OutClick/        # Хук для обработки кликов вне элемента
│   │   ├── Popup/           # Всплывающие уведомления
│   │   ├── Profile/         # Модальное окно профиля
│   │   ├── SearchUserInput/ # Поле поиска пользователей
│   │   ├── SearchUsers/     # Компонент поиска пользователей
│   │   ├── SidePanel/       # Боковая панель навигации
│   ├── context/             # Контексты React
│   │   ├── chatProvider.js  # Контекст чатов
│   │   ├── socketProvider.js # Контекст WebSocket
│   │   ├── userProvider.js  # Контекст пользователя
│   ├── hooks/               # Пользовательские хуки
│   │   └── useComponentVisible.js # Хук для отслеживания кликов
│   ├── images/              # Статические изображения
│   ├── utils/               # Вспомогательные функции
│   │   ├── chatData.js      # Обработка данных чатов
│   │   ├── createImadeBuffer.js # Создание буфера изображений
│   │   └── resSetUser.js    # Обработка данных пользователя
```

### Backend (server/)

```
socket-chat/server/
├── config/                  # Конфигурационные файлы
│   ├── db.js               # Подключение к MongoDB
│   └── generateToken.js    # Генерация JWT токенов
├── controllers/            # Контроллеры бизнес-логики
│   ├── chatController.js   # Управление чатами
│   ├── messageController.js # Управление сообщениями
│   └── userController.js   # Управление пользователями
├── middlewares/            # Промежуточное ПО
│   ├── authMiddleware.js   # Аутентификация JWT
│   ├── cors.js            # Настройка CORS
│   └── errorMiddlewares.js # Обработка ошибок
├── routes/                 # Маршруты API
│   ├── chatRoutes.js      # Маршруты чатов
│   ├── messageRoutes.js   # Маршруты сообщений
│   └── userRouter.js      # Маршруты пользователей
├── scheme/                 # Модели MongoDB
│   ├── chatSchema.js      # Схема чатов
│   ├── messageSchema.js   # Схема сообщений
│   └── userschema.js      # Схема пользователей
├── socket/                 # Логика WebSocket
│   └── socket.js          # Обработчики событий Socket.io
└── validation/            # Валидация данных
    ├── chatValidations.js # Валидация чатов
    ├── userValidations.js # Валидация пользователей
    └── validateRequest.js # Общая валидация
```

## 🔧 Установка и запуск

### Предварительные требования

- Node.js (версия 16 или выше)
- MongoDB (локально или через MongoDB Atlas)
- npm или yarn

### Установка и запуск клиента

```bash
# Переход в директорию клиента
cd socket-chat/client

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm start

# Сборка для production
npm run build
```

### Установка и запуск сервера

```bash
# Переход в директорию сервера
cd socket-chat/server

# Установка зависимостей
npm install

# Настройка переменных окружения
# Создать файл .env и добавить:
PORT=5000
MONGO_URI="mongodb://localhost:27017/socket-chat"
JWT_SECRET="your-secret-key"

# Запуск сервера
npm start

# Или в режиме разработки с nodemon
npm run dev
```

## 🚀 Основные особенности реализации

### WebSocket Реализация

- **Socket.io** для двусторонней связи в реальном времени
- **Комнаты чатов** для изоляции сообщений между разными беседами
- **События в реальном времени**: новые сообщения, набор текста, обновления чатов

### Безопасность

- **JWT токены** для аутентификации с сроком действия 30 дней
- **Валидация входных данных** на стороне сервера
- **Хеширование паролей** с использованием bcrypt
- **Защита маршрутов** с middleware аутентификации

### Оптимизация

- **Оптимизация изображений** через буферизацию
- **Ленивая загрузка** сообщений
- **Мемоизация** компонентов React
- **Дебаунсинг** поисковых запросов

### UI/UX

- **Адаптивный дизайн** для различных устройств
- **Индикаторы загрузки** для улучшения восприятия
- **Анимации переходов** и взаимодействий
- **Валидация форм** в реальном времени

## 📊 Структура данных

### Пользователь (User)

```javascript
{
  _id: ObjectId,
  name: String,          // Имя пользователя
  email: String,         // Email (уникальный)
  password: String,      // Хешированный пароль
  image: {               // Аватар
    data: Buffer,
    contentType: String
  },
  unreadMessages: Object // Непрочитанные сообщения по chatId
}
```

### Чат (Chat)

```javascript
{
  _id: ObjectId,
  chatName: String,      // Название чата
  isGroupChat: Boolean,  // Тип чата
  users: [ObjectId],     // Участники чата
  lastedMessage: ObjectId, // Последнее сообщение
  groupAdmin: ObjectId   // Администратор группы
}
```

### Сообщение (Message)

```javascript
{
  _id: ObjectId,
  sender: ObjectId,      // Отправитель
  content: String,       // Текст сообщения
  chat: ObjectId         // Чат
}
```

## 🔌 API Endpoints

### Аутентификация

- `POST /user/` - Регистрация нового пользователя
- `POST /user/login` - Вход пользователя
- `GET /user/` - Получение списка пользователей (с поиском)

### Чаты

- `POST /chat/` - Создание/доступ к личному чату
- `GET /chat/` - Получение всех чатов пользователя
- `POST /chat/group` - Создание группового чата
- `PUT /chat/rename` - Изменение названия группового чата
- `PUT /chat/groupremove` - Удаление пользователя из группы
- `PUT /chat/groupadd` - Добавление пользователя в группу

### Сообщения

- `POST /message` - Отправка сообщения
- `POST /message/:chatId` - Получение всех сообщений чата

### Профиль

- `PUT /user/image` - Обновление аватара
- `PUT /user/update` - Обновление данных пользователя

## 🎨 Особенности интерфейса

### Компоненты

1. **ChatBox** - Основное окно чата с историей сообщений и полем ввода
2. **MyChats** - Боковая панель со списком чатов пользователя
3. **GroupChatModal** - Модальное окно управления групповыми чатами
4. **Profile** - Модальное окно редактирования профиля
5. **SearchUsers** - Панель поиска пользователей

### Интерактивные элементы

- **Индикатор набора текста** с анимацией точек
- **Отображение непрочитанных сообщений** с счетчиком
- **Автоматическая прокрутка** к новым сообщениям
- **Валидация форм** в реальном времени

## 🔒 Безопасность и валидация

### Валидация на клиенте

- Проверка email формата
- Валидация пароля (длина, сложность)
- Подтверждение пароля при регистрации
- Валидация имени пользователя

### Валидация на сервере

- Express Validator для всех входящих данных
- Проверка существования пользователей
- Проверка прав доступа к чатам
- Валидация загружаемых файлов

## 📱 Особенности работы в реальном времени

### Socket.io События

- `setup` - Подключение пользователя к сокету
- `joinChat` - Подключение к комнате чата
- `leaveChat` - Выход из комнаты чата
- `newMessage` - Отправка нового сообщения
- `typingMessage` - Индикатор набора текста
- `renameChat` - Изменение названия чата
- `removeUserChat` - Удаление пользователя из чата
- `addUserChat` - Добавление пользователя в чат

## 👨‍💻 Автор

Павел Кулебин
